<div class="row">
  <div class="col-md-6 col-md-offset-3">
		<div class="panel panel-default">
	  	<div class="panel-body">
				<%= form_for @shout do |form| %>
					<%= form.text_field	:body, class: "form-control", placeholder: "SHOUT LOUD....." %>
					<%= form.submit 'Shout!', class: "btn btn-default" %>
				<% end %>
				<hr>
				<%= render @shouts %>
	  	</div>
		</div>
	</div>
</div>